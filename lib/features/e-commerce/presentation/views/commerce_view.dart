import 'package:flutter/material.dart';

class CommerceView extends StatelessWidget {
  const CommerceView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Welcome to the Commerce View!'),
      ),
    );
  }
}
